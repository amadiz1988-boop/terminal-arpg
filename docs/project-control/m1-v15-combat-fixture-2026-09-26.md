# M1 V15 single combat acceptance fixture

Status: `V15_STOPPED_AT_VER_EJU`. This is test acceleration for isolated TEST_PLAYER account `2000163`, character `150105`, group `0`, `is_test=1`. It supplies no gameplay PASS and changes no normal Player endpoint, combat rule, monster, map, level gate or Native binary.

## Previous ver_eju attack classification

The bounded Native log `20260926-213257-map.out.log` lines 1609–1651 has `AUTO_FARM_TARGET` for Recon Robot 3154 and three `AUTO_FARM_ATTACK` lines. Canonical Native `src/map/persistent_agent.cpp::issue_melee_attack` records those lines immediately before calling rAthena `unit_attack`. It does not log the return or weapon-damage resolution. There is no subsequent `AUTO_FARM_HIT` or authoritative monster HP decrease in the window. Thus PA accepted and issued attack intent; actual attack resolution is `UNKNOWN`, with no evidence distinguishing MISS from interrupt/no resolution. The target's canonical `db/re/mob_db.yml` row is Lv165, AGI60, DEX120, attack2989; live FLEE was not exported. Player base stats from normal authenticated `/api/state?view=full` are STR/AGI/VIT/INT/DEX/LUK `1/1/1/1/1/1`, unspent status points `68`, Base Lv170, job4190; effective HIT/FLEE/DEF were not projected. Equipped items are Knife 1201 and Cotton Shirt 2301.

## Frozen ordinary fixture

The single planned stat profile is STR80, AGI90, VIT90, INT20, DEX90, LUK20. Canonical `db/re/statpoint.yml` gives 3124 total points at Base Lv170. `src/map/pc.cpp::PC_STATUS_POINT_COST` Renewal formula charges 2115 for this profile, leaving 1009 unspent. Every stat is at most 90, within the canonical `conf/battle/player.conf` ordinary stat cap99. No per-map profile exists.

The fixed equipment bundle is unrefined, identified, cardless, unenchanted Magical Blade 13438 (right hand, ATK165, Base Lv105, `SuperNovice` permitted) and Awakened Eden Group Uniform V 15185 (armor, DEF100, Base Lv160, Jobs/Classes default All). Both are in canonical `db/re/item_db_equip.yml`. The armor adds general HP/stat bonuses; its record does not provide map-specific targeting or invulnerability. The item data and Native `pc_equipitem` decide final legality. No recovery consumable is provisioned and no map-specific equipment is selected.

`scripts/m1-v15-combat-fixture.mjs` defaults to read-only preflight. Explicit `--action prepare --execute true` requires clean GitHub-main source, the same F lease, exact Native/Web binding, one healthy runtime, TEST_PLAYER idle ownership, a current level fixture and item absence. It captures full character/inventory preimages, stops only the canonical Native runtime, compare-and-swap provisions legal stats and two test-only inventory rows, restarts Native, then uses ordinary authenticated Player `/api/item-action` equip commands. The already authorized fixed Native M1 profile resets HP/SP after equip; authoritative Player state must show the two slots, full HP/SP and `PERSISTENT_IDLE`. Inserted inventory IDs and profile are recorded under the ignored Production fixture-evidence directory. Explicit restore unequips test items through normal Player commands, re-equips preimage items, removes only the exact inserted rows offline and restores exact prior stats. Gameplay deltas outside the fixture are preserved.

The evidence rule remains: fixture setup is not combat PASS. Each of the five maps requires a normal group-0 Player request, authoritative arrival, AUTO_FARM, TARGET, attack resolution and one monster-HP-decrease HIT. Low-HP recovery is independently classified later. Settings, capability closure, Heimdall and final promotion remain separate gates.

## V15 live result and stop

Normal group-0 `POST /api/grind-target` to `ver_eju` returned scenario PASS for arrival, target save and AUTO_FARM, trace `scenario-change-farm-map-b7526402-568d-4fb6-b6d0-e56a16cc6eac`, command `89639b7c-bae8-4a14-8270-c4f7da3b0bf8`. The bounded observer started before the request and saw TARGET and two `AUTO_FARM_ATTACK` attempts; no `AUTO_FARM_HIT` or monster HP decrease occurred within 30 seconds. Exact current Native log: Production `.local/ro-stack/logs/20260926-223000-map.out.log`, `AUTO_FARM_TARGET` line 389, `AUTO_FARM_ATTACK` lines 406 and 408, `CHAR_HP_DECREASE` 10930→6917 damage 4013 line 409, `RECOVERING_ENTER` at the 70% threshold line 410, `RECOVERY_BLOCKED NO_LEGAL_RECOVERY_ACTION supply=UNREACHABLE` line 411, further incoming damage 4274 and 2643 lines 412 and 414, `DEAD_ENTER` line 415 and SavePoint respawn lines 416–420. A normal `stop_farm` then returned `PERSISTENT_IDLE`. Attack resolution remains `UNKNOWN`; the log does not distinguish MISS from interrupted or pending attack. The exact unmet transition is attack intent to authoritative monster HP decrease, followed by a factual recovery block and death. No ordinary recovery item was provisioned, so this run does not prove that a legal available recovery action was mishandled. Five-map live PASS count is 0/5; the four remaining maps, Settings Browser and capability final gate were not run under this stop condition.

The combat fixture's `restored.json` confirms the staged rows and stats were removed. The separate level fixture's `restored.json` confirms Base Lv7 Novice. Final authenticated Player state: group0/is_test1, STR/AGI/VIT/INT/DEX/LUK 1 each, status points68, Knife1201/Cotton Shirt2301, no staged items, prt_fild05, HP71/71, SP17/17, PERSISTENT_IDLE. Final Native snapshot passed with login/char/map each one, Dashboard and database each one, OpenKore0, ProcDump matching map PID. No new crash dump was found. Production Native/Web binaries were unchanged; the authorized test fixture altered and then restored only the scoped test character and restarted the canonical Native runtime for offline fixture setup/restore.

## Bounded start-snapshot recovery

The first preparation wrote the exact offline profile and two inventory rows, then restarted the canonical runtime. Its immediate post-start snapshot returned `COMBAT_FIXTURE_RUNTIME_RELOAD_FAILED`; a subsequent bounded snapshot showed all three servers healthy, OpenKore count 0, and ProcDump attached to the current map PID. Authenticated Player state confirmed the exact new stats and two unequipped rows. The tool now polls runtime convergence for at most 20 snapshots and provides an explicit `resume` path that verifies the failure record, preimage source SHA, exact staged IDs, legal stats and idle identity before equipping. It does not repeat offline writes or restart the runtime during resume.

Normal Player equip rejected both test items before dispatch: the Web inventory catalog does not classify IDs 13438 and 15185, so its read model presented `equippable=false`, `itemType=-1`, despite the canonical rAthena item database loading both records. The authorized test-only fallback `--action equip-offline --execute true` requires the exact staged IDs and original Knife/Cotton Shirt masks, stops the one Native runtime, atomically sets the legal equipment masks, restarts and waits for rAthena projection. After reload, authoritative inventory masks were 2 and 16 and max HP rose from 9330 to 10930; the Web `equipment` list remained empty because the same catalog classifies both IDs as `etc`. The explicit `confirm-offline` path checks the fresh Native inventory masks, exact staged DB rows, idle identity and full HP/SP before writing `prepared.json`, with no further restart. This does not create a Player API bypass. Restore reverses those masks and removes only staged rows under compare-and-swap checks. The Web catalog gap remains a separate Player item-display/equip capability finding and does not count as normal Player equip PASS.
