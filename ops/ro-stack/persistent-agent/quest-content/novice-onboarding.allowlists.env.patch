# Novice onboarding allowlist config patch (SOURCE-READY — do not deploy in this workline)
#
# Minimal exact additions required by the frozen 26-phase novice flow.
# Source of truth: novice-onboarding.spec.md + novice-onboarding.allowlists.json
# Native loader: persistent_agent.cpp:732-876 (env-driven allowlists).
# NO wildcards. FARM_UNTIL uses the existing AUTO_FARM handoff with mobId=0.
#
# Merge policy: append these comma-separated values to the existing env vars
# (union), never replace, never broaden beyond these exact objects.

PERSISTENT_AGENT_NPCS=terminal_ship_wounded,#ship_out,#intro_to_izlude,Captain Carocc#intro_npc03,Lumin#new_ship,Sailor#intro_npc04,Captain Carocc#iz,Criatura Academy Staff#0,Academy Receptionist#1,Adept Adventurer#ac,學院結業導師#terminal
PERSISTENT_AGENT_NPC_MAPS=iz_int,int_land,izlude,iz_ac01
PERSISTENT_AGENT_NAVIGATION_MAPS=iz_int,int_land,izlude,iz_ac01,new_1-3
PERSISTENT_AGENT_FARM_MAPS=new_1-3
PERSISTENT_AGENT_QUEST_IDS=21001,7471,21008,7472,7473,4269,2293
PERSISTENT_AGENT_QUEST_TASKS=7100001
PERSISTENT_AGENT_SERVICE_ITEMS=531,569
PERSISTENT_AGENT_QUEST_SEQUENCES=novice_onboarding
# PERSISTENT_AGENT_FARM_MOBS=      # intentionally empty: FARM_UNTIL uses mobId=0
# PERSISTENT_AGENT_QUEST_REWARD_ITEMS=   # empty: graduation rewards are granted by the academy NPC script, not CONFIRM_REWARD
